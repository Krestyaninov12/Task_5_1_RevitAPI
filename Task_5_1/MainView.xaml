<Window x:Class="Task_5_1.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Task_5_1"
             mc:Ignorable="d" 
             Height="180" Width="300"
        Title="Task_5_1">
    <StackPanel>
        <Label Content="Количество" Margin="5"/>
        <Button Content="Количество труб" Command="{Binding SelectCommand1}" Margin="5"/>
        <Button Content="Объем стен" Command="{Binding SelectCommand2}" Margin="5"/>
        <Button Content="Количество дверей" Command="{Binding SelectCommand3}" Margin="5"/>
    </StackPanel>
</Window>
